apiVersion: v2
name: notes-service
description: A Helm chart for the Just Put It Notes Service
type: application
version: 0.1.0
appVersion: "latest"

dependencies:
  - name: postgresql
    version: "12.x.x"  # Use whichever version you were using (Bitnami 12.x is standard)
    repository: "https://charts.bitnami.com/bitnami"